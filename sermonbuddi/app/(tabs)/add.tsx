import { View } from 'react-native';

const AddScreen = () => {
  // This screen is not meant to be visible.
  // The tab bar button for it opens a modal instead.
  return <View />;
};

export default AddScreen; 